# ── Butler AI backend — minimum production requirements ──────────────────
# Backend only ships /app/backend/server.py which uses FastAPI + Motor (MongoDB).
# The Emergent build container installs with `pip install --no-cache-dir
# --no-dependencies` so every transitive dep MUST be listed here explicitly.
#
# History: previous requirements.txt contained `jq>=1.6.0` (requires libjq-dev
# system library + gcc to compile, which broke the slim Docker image), plus
# 20+ unused packages (pandas, numpy, boto3, pytest, black, isort, flake8,
# mypy, typer, etc.). All removed.

# ── Web framework ────────────────────────────────────────────────────────
fastapi==0.110.1
starlette==0.37.2
uvicorn[standard]==0.25.0
python-multipart==0.0.9

# ── Data validation ──────────────────────────────────────────────────────
pydantic==2.6.4
pydantic-core==2.16.3
annotated-types==0.6.0
typing-extensions==4.12.2
email-validator==2.2.0

# ── MongoDB (async driver Motor + low-level PyMongo) ─────────────────────
motor==3.3.1
pymongo==4.5.0
dnspython==2.6.1

# ── Env loading ──────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Uvicorn runtime helpers ──────────────────────────────────────────────
anyio==4.4.0
sniffio==1.3.1
idna==3.7
h11==0.14.0
click==8.1.7
httptools==0.6.1
uvloop==0.19.0; sys_platform != 'win32'
watchfiles==0.21.0
websockets==12.0
pyyaml==6.0.1
